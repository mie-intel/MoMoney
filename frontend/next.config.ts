import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",
  basePath: "/MoMoney",
  assetPrefix: "/MoMoney/",
  reactCompiler: true,
};

export default nextConfig;
