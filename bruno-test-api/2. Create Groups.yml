info:
  name: Create Groups
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/groups/
  body:
    type: json
    data: |-
      {
        "name": "haiya",
        "columns": ["ea", "jaskdjaksd"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
