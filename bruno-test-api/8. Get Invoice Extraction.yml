info:
  name: Get Invoice Extraction
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8000/invoices/{{invoice_id}}/extractions

runtime:
  variables:
    - name: invoice_id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
